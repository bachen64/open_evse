name: Build/Release OpenEVSE

on:
  workflow_dispatch:
  push:
    branches:
      - linco-work
  pull_request:

jobs:
  info:
    name: Get event information
    runs-on: ubuntu-latest
    steps:
      - name: Dump context
        uses: crazy-max/ghaction-dump-context@v1


  build_platform_io:
    name: Build using PlatformIO
    uses: OpenEVSE/open_evse/.github/workflows/build_platformio.yml@linco-work

  build_arduino:
    name: Build using Arduino
    uses: OpenEVSE/open_evse/.github/workflows/build_arduino.yml@linco-work

  release:
    name: Create pre-release

    needs:
      - build_platform_io
    if: github.ref == 'refs/heads/linco-work'

    uses: OpenEVSE/open_evse/.github/workflows/release.yml@linco-work
